import React from 'react';
import ReactDOM from 'react-dom';
import './index.css';
import RPS from './RPS';

ReactDOM.render(
  <React.StrictMode>
    <RPS />
  </React.StrictMode>,
  document.getElementById('root')
);
